import React, { Component } from "react";
import "./App.css";

class Saved extends Component {
  render() {
    return (
      <div>
        <h1>Saved Page</h1>
      </div>
    );
  }
}

export default Saved;